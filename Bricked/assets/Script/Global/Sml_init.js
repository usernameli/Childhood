SML.init = function() {
    // SML.Share.init();
    // SML.AI.init();
    SML.WindowsManager.init();
    SML.TableMsgManager.init();
    SML.Receiver.init();
    SML.TodoTaskManager.init();
    SML.MapCheckPointList.init();
    // SML.FTSelected.init();
    // SML.GameoverManager.init();
};